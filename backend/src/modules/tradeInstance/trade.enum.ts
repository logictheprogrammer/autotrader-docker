export enum TradeStatus {
  PREPARING = 'preparing',
  RUNNING = 'running',
  ON_HOLD = 'on hold',
  MARKET_CLOSED = 'market closed',
  SETTLED = 'settled',
}

export enum TradeMove {
  LONG = 'long',
  SHORT = 'short',
}
